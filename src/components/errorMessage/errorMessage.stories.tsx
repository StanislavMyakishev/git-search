import React from 'react';

import ErrorMessage from './index';

export default {
    title: 'Error message',
    component: ErrorMessage,
};

export const Basic = (): React.ReactElement => <ErrorMessage />;
